// Shared via Compiler App https://g633x.app.goo.gl/TPlw
#include<stdio.h>
void main()
{
	int a=5;
	printf("the address of a is:%d",&a);
	// to print address & operator is used &&a is not valid because it means &(&a) i.e &(constant value)

	// also we can subtract two address, add,div,mult is not possible
}